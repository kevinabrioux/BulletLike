def common_pods
  pod 'FirebaseCore'
  pod 'FirebaseAuth'
  pod 'FirebaseDatabase'
  pod 'FirebaseStorage'
end

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BulletLike (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BulletLike (iOS)
  common_pods

end

target 'BulletLike (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BulletLike (macOS)
  common_pods
end

target 'Tests iOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for BulletLike (macOS)
  common_pods
end
